/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/nextjs_wrid_official_website',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
